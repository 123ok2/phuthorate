
import { User, Agency, EvaluationCycle, Evaluation, Region } from './types';

// Dữ liệu ảo đã bị loại bỏ hoàn toàn. 
// Hệ thống hiện tại sẽ khởi tạo từ dữ liệu thực tế trên Firebase Firestore.

export const regions: Region[] = [];
export const agencies: Agency[] = [];
export const cycles: EvaluationCycle[] = [];
export const mockUsers: User[] = [];
export const mockEvaluations: Evaluation[] = [];
